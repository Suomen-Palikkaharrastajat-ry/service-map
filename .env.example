VITE_POCKETBASE_URL=https://data.palikkaharrastajat.fi
POCKETBASE_URL=https://data.palikkaharrastajat.fi

# Vector basemap style served by the service-map-tiles project. The value below
# is production and is also the built-in default — change it only to point at a
# staging tiles deployment or a locally served copy.
VITE_BASEMAP_STYLE_URL=https://tiles.palikkaharrastajat.fi/style.json
